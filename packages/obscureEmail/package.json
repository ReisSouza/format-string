{
  "name": "@format-string/obscure-email",
  "version": "1.0.1",
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "dev": "tsup src/index.ts --format esm,cjs --dts --watch",
    "lint": "eslint \"src/**/*.ts*\""
  },
  "devDependencies": {
    "@format-string/eslint-config": "*",
    "@format-string/tsconfig": "*",
    "eslint": "^7.32.0",
    "typescript": "^4.5.3",
    "tsup": "^6.7.0"
  }
}
